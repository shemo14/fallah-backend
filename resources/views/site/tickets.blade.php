@extends('site.layouts.index')@section('title')    | {{ trans('site.tickets') }}@endsection@section('content')    <div class='content'>        <div class="container contentContainer">            @include('site.layouts.top_header')            <div class="category">                <div class="container">                    <div class="events">                        <h4 class='title'>{{ trans('site.tickets') }}</h4>                        <div class="row">                            @foreach($bookings as $booking)                                <div class="col-md-4">                                    <a href='{{ route('booking_details', $booking->id) }}' class="eventDet wow fadeInUp " data-wow-delay="1.2s">                                        <img src="{{ Request::root() }}/images/events/{{ $booking->event->images()->first()->name }}" alt="event Img">                                        <span class="date">{{ Carbon\Carbon::parse($booking->event->date)->format('M d') }}</span>                                        <span class="name">{{ $booking->event->title }}</span>                                    </a>                                </div>                            @endforeach                        </div>                        <nav aria-label="Page navigation example">                            {{ $bookings->links() }}                        </nav>                    </div>                </div>            </div>        </div>    </div>@endsection