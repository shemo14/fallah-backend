@extends('site.layouts.index')@section('title')    | {{ trans('site.saves') }}@endsection@section('content')    <div class='content'>        <div class="container contentContainer">            @include('site.layouts.top_header')            <div class="category">                <div class="container">                    <div class="events">                        <h4 class='title'>{{ trans('site.saves') }}</h4>                        <div class="row">                            @foreach($events as $event)                                <div class="col-md-4">                                    <a href='{{ route('event', $event->id) }}' class="eventDet wow fadeInUp " data-wow-delay="1.2s">                                        <img src="{{ Request::root() }}/images/events/{{ $event->images()->first()->name }}" alt="event Img">                                        <span class="date">{{ Carbon\Carbon::parse($event->date)->format('M d') }}</span>                                        <span class="name">{{ $event->title }}</span>                                    </a>                                </div>                            @endforeach                        </div>                        <nav aria-label="Page navigation example">                            {{ $events->links() }}                        </nav>                    </div>                </div>            </div>        </div>    </div>@endsection